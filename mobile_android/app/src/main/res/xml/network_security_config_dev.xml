<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuração exclusiva do flavor "dev": cleartext continua bloqueado por
    padrão (base-config), mas é liberado apenas para hosts locais explícitos
    usados durante desenvolvimento (emulador Android e localhost). Nenhum
    domínio de homologação ou produção aparece aqui — nunca aceitar
    certificado inválido, nunca desabilitar validação TLS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
